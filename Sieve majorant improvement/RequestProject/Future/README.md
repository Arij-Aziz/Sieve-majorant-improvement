# Future Work Scaffolding

**Nothing in this directory is part of the proof chain for the three core theorems.**

Every file here is either:
- Infrastructure for future research directions not yet formalized, or
- Marked `AssumedForProgram` in `AssumptionsRegistry.lean` (conditional on
  unverified hypotheses).

| Directory | Status |
|-----------|--------|
| `Dynamics/` | ProvedInProject; not used in Papers 1–3 |
| `Hybrid/` | ProvedInProject; not used in Papers 1–3 |
| `Models/` | ProvedInProject; not used in Papers 1–3 |
| `Applications/` | AssumedForProgram — do not treat as verified results |

The core theorems live in `../Core/`.
